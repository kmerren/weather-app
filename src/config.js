/**
 * Default location configuration for weather display
 * 
 * To change the location:
 * 1. Update the city and country names
 * 2. Provide the correct latitude and longitude
 * 3. Save the file
 * 
 */
export const DEFAULT_LOCATION = {
  // Display names
  city: "Paris",          // City name to display
  country: "FR",          // Country code (2 letters)
  
  // Geographic coordinates
  latitude: 48.8566,      // Latitude in decimal degrees
  longitude: 2.3522,      // Longitude in decimal degrees
};
